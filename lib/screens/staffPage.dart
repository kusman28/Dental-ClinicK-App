import 'package:flutter/material.dart';

class Staff extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(title: Text('Admin'),),
      body: new Column(
        children: <Widget>[
          new Text('Sana ol'),
        ],
      ),
    );
  }
}